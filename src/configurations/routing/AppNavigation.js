const appRoutes = {
  authentication: {
    login: '/login',
  },
};

const defaultAuthenticatedRoute = '/';
const defaultUnauthenticatedRoute = '/login';

export { appRoutes, defaultAuthenticatedRoute, defaultUnauthenticatedRoute };
